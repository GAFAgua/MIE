# 羊纹杏形金叶网页版本

这个项目保存了三个静态网页版本：

- `sites/archive/`：档案入口版，默认进入 `#archive`。
- `sites/model/`：3D 主展示版，默认展示 3D 模型。
- `sites/pattern/`：纹样入口版，默认进入 `#pattern`。

本地预览可使用：

```bash
python3 -m http.server 4173 --bind 0.0.0.0
```

同一局域网下可用电脑 IP 访问。
